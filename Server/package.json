{
  "name": "irc-test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "docker-compose -f ./docker/dev.yml up",
    "alex": "babel src --out-dir dist --source-maps inline --copy-files && nodemon --exec babel-node ./src/app.js DEV",
    "watch": "babel --watch src --out-dir dist --source-maps inline --copy-files",
    "build": "babel src --out-dir dist --source-maps inline --copy-files",
    "prod": "docker-compose -f ./docker/prod.yml up",
    "test": "docker-compose -f ./docker/test.yml up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2",
    "crypto": "0.0.3",
    "express": "^4.15.0",
    "mongodb": "^2.2.23",
    "redis": "^2.6.5",
    "screenshot": "^0.1.0",
    "shortid": "^2.2.6",
    "socket.io": "^1.7.2"
  },
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "nodemon": "^1.11.0",
    "should": "^11.2.0",
    "sinon": "^1.17.7"
  }
}
